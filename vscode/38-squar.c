/*accept a values if that will be odd then print the squar of accepted value*/
#include<stdio.h>
#include<conio.h>
void main()
{
    int a;
    printf("emter a values:-");
    scanf("%d",&a);
    if("a%2==1")
    printf("a squar value=%d",a*a);
    getch();
}